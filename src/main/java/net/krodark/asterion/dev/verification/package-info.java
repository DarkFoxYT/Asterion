/**
 * Development checks compiled with the main project, alongside the template builders in
 * {@code net.krodark.asterion.dev}. These executables are not called during gameplay.
 * Run {@code gradlew centipedeRegression}, {@code gradlew catacombRegression}, or
 * {@code gradlew check} from the project root. The checks read the editable source assets.
 */
package net.krodark.asterion.dev.verification;
